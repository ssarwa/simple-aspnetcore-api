apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aspcorechart.fullname" . }}-test-connection"
  labels:
    {{- include "aspcorechart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "aspcorechart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
